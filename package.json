{
  "name": "chatgpt-to-json",
  "version": "1.0.0",
  "description": "Scrape shared ChatGPT conversations to JSON",
  "main": "scrape.js",
  "scripts": {
    "start": "node scrape.js",
    "install-browsers": "playwright install chromium --quiet",
    "install-deps": "playwright install-deps --quiet 2>/dev/null || echo \"System dependencies installed (if needed)\"",
    "setup": "npm install && npm run install-browsers && npm run install-deps"
  },
  "keywords": ["chatgpt", "scraper", "json"],
  "author": "P-O Yliniemi",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
